// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: Open Default Keymap`
// from the command palette.
[
  {
    "context": "Workspace",
    "bindings": {
      "shift shift": "file_finder::Toggle",
      "ctrl ctrl": "command_palette::Toggle"
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      // Prevent closing the project pane when pressing cmd-w with a tab in preview mode!
      // => https://github.com/zed-industries/zed/issues/45261
      "cmd-e": "editor::ToggleFocus",
      "cmd-w": ["workspace::SendKeystrokes", "cmd-e cmd-w"]
    }
  },
  {
    "context": "Editor",
    "bindings": {
      // "j k": ["workspace::SendKeystrokes", "escape"]
      "cmd-backspace": "editor::DeleteLine",
      "cmd-alt-left": ["workspace::SendKeystrokes", "cmd-shift-e cmd-left cmd-shift-e"]
    }
  }
]
