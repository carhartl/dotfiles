function g {
	if [[ "$*" == "root" ]]; then
		local root
		root="$(git rev-parse --show-toplevel)" || return $?
		cd "$root" || return $?
	elif [[ $# > 0 ]]; then
		git "$@"
	else
		echo "Last commit: $(time-since-last-commit) ago"
		git status --short --branch
	fi
}
