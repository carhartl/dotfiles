slate.on("windowOpened", function (event, win) {
    if (win.app().name() === "Kaleidoscope") {
        win.move({ "x": "screenOriginX+((screenSizeX-windowSizeX)/2)", "y": "screenOriginY+((screenSizeY-windowSizeY)/2)" });
    }
});
