#!/usr/bin/env bash
#
# Send notification to macOS notification center.
# Usage: run_some_long_running_process ; notify 'all done'

set -e
set -u
set -o pipefail

osascript <<EOF
display notification "$(iso8601)" with title "${1-Achtung}"
EOF
